# Flask Server
A simple Flask Server that can receive and show data feed of one sensor

## Setup
Install python requirements packages:
```shell
pip install -r requirements.txt
```
## Config File
Need to write a config.ini file to run the bridge. An example below:
```ini
[Flask]
FLASK_RUN_HOST = 0.0.0.0
FLASK_RUN_PORT = 9000

[SQLAlchemy]
SQLALCHEMY_DATABASE_URI = sqlite:///db.sqlite
```
## Documentation
Documentation has been generated by Swagger, it's available from:
```text
/api/docs
```